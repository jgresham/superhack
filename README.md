# superhack

helpful aliases

```shell
alias pp='podman pod'
alias pk='podman kube'
alias ppl='pp logs -f --tail 100 op-node-pod'
```

helpful commands
`pk play op-node.yaml`
`ppl`
`pp rm op-node-pod -f`
